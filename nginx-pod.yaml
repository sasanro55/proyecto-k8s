apiVersion: v1
kind: Pod
metadata:
  name: new-nginx-pod
  labels:
    app.kubernetes.io/name: new-nginx-pod
spec:
  containers:
  - name: new-nginx-pod
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
